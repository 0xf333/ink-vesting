[package]
name = "vesting"
version = "0.1.5"
authors = ["0xf333 <@0xf333:matrix.org>"]
edition = "2021"

[dependencies]
ink = { version = "5.0.0", default-features = false }

[dev-dependencies]

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "ink/std"
]

ink-as-dependency = []

